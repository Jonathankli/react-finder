{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build --scope @jkli/react-finder",
    "watch": "lerna run watch --scope @jkli/react-finder",
    "start": "lerna run start --scope react-finder-example",
    "test": "lerna run test --scope @jkli/react-finder",
    "test:watch": "lerna run test:watch --scope @jkli/react-finder"
  },
  "devDependencies": {
    "lerna": "^6.0.1"
  }
}
